# 仕様書駆動開発 (SDD) AI行動規範 & ガードレール (GEMINI.md)

本ドキュメントは、Google Codelabsの仕様書駆動開発（SDD: Specification-Driven Development on Antigravity / ADK）をベースとし、「現代魔獣・幻獣生態図鑑プロジェクト」においてAI（Antigravity / Gemini）が設定構築、対話、魔獣生成、リファクタリングを行う際の行動規範、ガードレール、対話プロトコルを定めたものです。

---

## 1. 仕様書駆動開発 (SDD) ワークフロー原則

本プロジェクトのすべての新規設定、魔獣エントリー、ルール策定、リファクタリングは、以下のフェーズおよび**決定論的ゲートキーパーシステム（3大承認ゲート）**を厳格に経て実行されます。

```
[Phase 0: Specify] ── 外部科学調査・古典文献考証・原典発祥地マッピングの仕様書起草 (.specify/specs/*.spec.md)
       ▼
【Gate 1: 仕様すり合わせ承認 (Spec HALT)】 ★ここで停止しユーザー合意
  ├ ① 🐾5専門部署ペルソナ＆⚖️憲章監査室の事前審査見解の開示
  ├ ② 方向性・生態モデルの選択肢（A/B/C/X）の提示と合意
  ├ ③ ★【和名複数選択肢の提示・選択】命名規約に基づく和名候補（形態/環境/季節/伝承型）の提示とユーザー決定
  ├ ④ 機械的仕様バリデータ実行 (engine/sdd/run_gate.ps1 -Id <id> -Gate 1) ➔ 100% PASS確認
  └ ⑤ 人間承認トークン発行 (.specify/approvals/<id>_gate1.json) ＆ 正史記録 (Q&A, audit.log)
       ▼（gate1.json の存在が確認されるまで Phase 2 移行を物理ブロック）
[Phase 2: Plan]    ── 実装計画書の策定 (.specify/plans/*.plan.md)
[Phase 3: Tasks]   ── 独立検証可能なタスクリスト作成 (.specify/tasks/*.tasks.md)
[Phase 4: Analyze] ── 機械的計画バリデータ実行 (engine/sdd/run_gate.ps1 -Id <id> -Gate 2) ➔ 100% PASS確認
       ▼
【Gate 2: 計画承認・ペルソナ査定 (Plan HALT)】 ★ここで停止しユーザー承認
  ├ ① 📋 5専門部署ペルソナ別チェックマトリックス・Grep客観エビデンスの展開
  ├ ② 🎭 5専門部署ペルソナ別ロールプレイ査定報告の開示
  ├ ③ 🧐 詳細設計の確認質問（主級Alpha、メガコーポ動態等）の提示
  ├ ④ ★【計画着工承認（Proceed）の受領】質問回答とは独立して「計画承認」を受領して停止（HALT）
  └ ⑤ 人間承認トークン発行 (.specify/approvals/<id>_gate2.json)
       ▼（gate2.json の存在が確認されるまで Phase 5 移行を物理ブロック）
[Phase 5-A: QGIS生成] ── QGIS 3.44 LTR タクティカルマップ自動レンダリング
       ▼
【Gate 2.5: QGIS画像目視承認 (QGIS HALT)】 ★ここで機械的に停止し人間目視承認
  ├ ① 機械的画像バリデータ実行 (engine/sdd/run_gate.ps1 -Id <id> -Step qgis) ➔ >100KB・PNG整合確認
  ├ ② 生成された戦術マップ画像の提示と人間による目視確認
  ├ ③ ★ ユーザーによる画像承認（Proceed / リテイク指示）の受領
  └ ④ QGIS承認トークン発行 (.specify/approvals/<id>_qgis.json)
       ▼（qgis.json の存在が確認されるまで 本稿執筆・画像生成を物理ブロック）
[Phase 5-B: Implement] ── 3大写真生成および図鑑記事執筆（TEMPLATE.md 全9章全20節）
       ▼
[Phase 6: Audit]   ── 管理ドキュメント同期 (README.md, current_status.md, audit.log)
```

---

## 2. 絶対遵守コミュニケーションプロトコル

### 2.1 関連ドキュメントの事前チェック義務
- AIは作業を開始する前に、必ず関連する既存ドキュメント（`AGENTS.md`、`.specify/memory/constitution.md`、`world/*.md`、`doc/questions_and_answers.md`、`creatures/*.md` 等）を事前に全確認すること。

### 2.2 3層防御ゲートキーパーシステム（物理ブロック原則）
AIの自律判断による「省略・先走り（HALTスキップ）」を決定論的に防止するため、以下の3層防御を絶対義務とする：
1. **第1層：機械的バリデータ（Static Validators）**:
   - Gate 1: `engine/sdd/run_gate.ps1 -Id <id> -Gate 1`（見出し、原典、一次生息地、和名、分類、Class）
   - Gate 2: `engine/sdd/run_gate.ps1 -Id <id> -Gate 2`（全20節見出し、GURPS公式呪文実在自動grep、上位存在固有名詞・鳴き声/精霊語音素検証、QGIS定義、6ペルソナ）
   - QGIS: `engine/sdd/run_gate.ps1 -Id <id> -Step qgis`（実在、サイズ>100KB、PNG整合）
   - ※バリデータが 100% PASS しない限り、承認要請を行ってはならない。
2. **第2層：物理的承認トークン（Approval Gatefiles）**:
   - 各ゲートを通過する際、`.specify/approvals/<id>_<step>.json` の実在を `assert_approval.py` で検証する。
   - 承認トークンが存在しない場合、後続フェーズのツール実行はスクリプトにより Exit Code 1 で物理的にブロックされる。
3. **第3層：対話プロトコルの完全分離（「質問への回答」≠「着工承認」）**:
   - ユーザーが質問に答えた段階では**着工承認とはみなさず**、計画・検証結果を提示して「この計画で実装を開始してよろしいですか？（Proceed確認）」と尋ねて**必ず停止（HALT）**すること。

### 2.3 Gate 1（仕様合意・和名選択）プロトコル
- **方向性合意の次に、必ず和名の複数選択肢を提示すること**:
  - 生態モデルや出現経緯の方向性合意に続き、標準和名命名規約（形態型、環境型、季節型、伝承型）に基づく**複数の標準和名候補を提示し、ユーザーに選択・決定してもらうこと**。
  - 和名決定を経て `verify_gate1.py` を実行し、PASS確認後にユーザーの承認を得て Gate 1 承認トークンを発行する。

### 2.4 Gate 2（計画承認・ペルソナ査定）完全展開プロトコル
Gate 2の提示時には、単に要約するのではなく、以下の4大要素をユーザーメッセージ内に**完全展開（Full Disclosure）**して停止（Gate 2 HALT）すること：
1. **📋 実装計画チェックリスト ＆ 機械的突合エビデンス（Plan Checklists & Grep Evidence）の全文展開**:
   - `verify_gate2.py` による全20節見出し網羅、GURPS公式呪文アーカイブ（`world/magic/spells/*.md`）実在突合行番号、上位存在（動物王の鳴き声・精霊王の精霊言語）の固有名詞・プレーンパクト設計、QGISタクティカルマップ生成定義。
2. **🎭 6専門部署ペルソナ別ロールプレイ報告**:
   - ゼクスト、鷹司、陣内、シュルツ、黒田、蓮見の6名全員による専門的査定コメント。
3. **🧐 詳細設計の確認質問（Q1/Q2: A/B/C/X）**:
   - 主級変異（Alpha）やメガコーポ動態などの詳細分岐点。
4. **★ 計画着工承認（Proceed）の受領待ち停止（HALT）**:
   - AIは実装ツール（QGIS、画像生成、ファイル作成）を一切呼び出さず、ユーザーの承認（Proceed）を待つ。

### 2.5 Gate 2.5（QGIS画像機械的停止・目視承認 ＆ オンデマンド通信・完全遮断検証）プロトコル
- **オンデマンド外部通信 ＆ 機械的遮断検証ルール（サンドボックス安全性担保）**:
  - サンドボックスの安全性を維持するため、QGIS地図作成に必要な高解像度タイル取得時のみ一時的に外部通信の口を開く（`fetch_map_tiles.py` を `BypassSandbox: true` で単独実行）。
  - 通信完了後、直ちに外部通信が遮断されていることを標準サンドボックス環境（`BypassSandbox: false`）で機械的に検証する（`assert_network_isolated.py`）。
  - **機械的遮断チェックが 100% PASS（Exit 0）するまで、地図レンダリングおよび後続のいかなる処理も進めてはならない（`generate_tactical_map.py` 内でも自動隔離アサーションを実行し、万一外部接続が可能な状態であれば実行を即時拒否・異常終了する）**。
- **完全オフライン描画（BypassSandbox: false）**:
  - タイルおよびジオリファレンス（`.pgw`/`.prj`）がローカルに揃った状態で、サンドボックス内（`BypassSandbox: false`）にて `generate_tactical_map.py` を完全オフライン実行する。
- **QGIS画像生成直後の機械的停止義務（QGIS HALT）**:
  - QGISタクティカルマップを実行した後、直ちに記事執筆や画像生成へ進むことを**絶対厳禁**とする。
  - `verify_qgis_map.py` で画像の正常性（>100KB、PNGヘッダ）およびネットワーク隔離を検証後、**生成された戦術マップ画像をメッセージ内に提示し、ユーザーに目視確認を求めて機械的に作業を停止（QGIS HALT）すること**。
  - ユーザーからの「マップ承認（Proceed）」を受領して初めて `qgis` 承認トークンを発行し、本稿執筆（Phase 5-B）へ進む。

### 2.6 GURPS公式呪文・生体異能 Grep突合義務（クリスティナ規定）
- **架空呪文・別ゲーム用語の混入厳禁**: 魔導科学部（およびAI）が図鑑記事や設定に呪文《〇〇》や生体異能（Diffuse等）を記述する際は、必ず `world/magic/spells/*.md` および `world/magic/rules/*.md` に対して `grep_search` を実行し、**公式アーカイブ内に実在することを確認した上で引用すること**。

### 2.7 上位存在（動物王・精霊王）命名・プレーンパクト突合義務（エルリック規定）
- **上位存在の固有名詞策定**: Plan（および記事本稿）の第5.3節において、アストラル界の上位存在（動物王／精霊王／上位神性）の真名・固有名詞を必ず策定すること。
  - **動物のアーキタイプの王（Animal Lord）**: その生物たちが呼べる名前、すなわち「該当動物自身の発声器官・鳴き声（咽頭ヒス音、遠吠え、唸り声、クリック音、呼気摩擦音等）」の音素構造による固有名詞であること。
  - **精霊の王・領主（Elemental Lord）**: 精霊界の言語（Elemental Tongue）による音韻構造であること。
  - **機械的突合**: `verify_gate2.py` は、固有名詞（『...』）および発声・音素由来（ヒス音、遠吠え、鳴き声、精霊語等）の記述が Plan に存在するかを機械的に検証し、欠落している場合は Gate 2 で物理ブロック（Exit 1）する。

### 2.8 ツール実行エラー・クォータ枯渇時の即時停止義務
- 画像生成（`generate_image`）やWeb検索などの外部ツール実行時に、APIレートリミット・クォータ枯渇（HTTP 429 Too Many Requests等）や致命的エラーが発生した場合、**最後まで勝手に作成して完了扱いとすることを厳禁とし、直ちに作業を停止（BLOCK / HALT）すること**。



---

## 3. 世界観整合性ガードレール (憲章5大観点)

1. **進化・時間軸（26年）**: 2000年マナ覚醒から約26年の時間軸で成立するメカニズム（急速変異/古代覚醒/異界流入）。
2. **科学・魔術並行＆生体媒介**: 電子機器と非干渉。マナは生体/精神/魂のみに作用。
3. **軍事・弾道力学バランス**: 通常兵器（5.56mm/7.62mm/12.7mm）で通常魔獣は駆逐可能。障壁は重火器・物理運動エネルギーで突破可能。
4. **日常と非日常の境界線**: 結界都市・要塞防護線・アウトランドの地理境界、メガコーポ支配、新人類（メタヒューマン）差別。
5. **生物・食・素材のリアリズム**: 三段階分類（Class-A/B/C）遵守、現実の解剖学・衛生・解体手順に基づく記述。

---

## 4. プロジェクト管理・ディレクトリ構造

- `.specify/`: 仕様書駆動開発のコア（`memory/constitution.md`, `specs/`, `plans/`, `tasks/`, `templates/`）
- `.agents/`: Antigravity エージェント資産（`rules/`, `workflows/`, `skills/`）
- `world/`: 正史世界観設定（地理、歴史・社会、魔導理論・全16ルール・全15系統呪文）
- `creatures/`: `TEMPLATE.md` に基づく個別魔獣エントリー
- `doc/`: 監査ログ（`audit.log`）、合意事項（`questions_and_answers.md`）、進捗（`current_status.md`）
